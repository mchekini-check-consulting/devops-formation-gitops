apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: pgcat
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - env: dev
            namespace: dev
          - env: qualif
            namespace: qualif
          - env: prod
            namespace: prod
  template:
    metadata:
      name: "pgcat-{{.env}}"
    spec:
      project: default
      sources:
        - repoURL: https://github.com/mchekini-check-consulting/devops-formation-utils.git
          targetRevision: feat/postgres
          path: k8s/helm/pgcat/
          helm:
            valueFiles:
              - $values/environments/{{.env}}/10-operators/pgcat/values.yaml
        - repoURL: https://github.com/mchekini-check-consulting/devops-formation-gitops.git
          targetRevision: feat/postgres
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
