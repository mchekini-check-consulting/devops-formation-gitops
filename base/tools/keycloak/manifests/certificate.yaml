apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-cert
spec:
  secretName: keycloak-tls
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  ipAddresses:
    - "51.11.203.15"
