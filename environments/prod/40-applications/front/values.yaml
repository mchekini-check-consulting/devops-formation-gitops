image:
  tag: 9476253-main-20260826115754 # sha-slugbranch-timestamp
namespace: prod
replicaCount: 1
resources:
  requests:
    cpu: 15m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
certificate:
  enabled: true
  name: front-prod-cert
  secretName: front-prod-tls
  issuer: selfsigned-issuer
  dnsNames:
    - "a76e1394f9b474a11bf71d91e5cfd3ef-616597816.us-east-1.elb.amazonaws.com"
    - "shop.check-consulting.net"
configmap:
  CATALOG: "https://shop.check-consulting.net/api/products"
  ORDERS: "https://shop.check-consulting.net/api/orders"
  PAYMENT: "https://shop.check-consulting.net/api/payments"
  AUTH_ENABLED: "true"
  KEYCLOAK_URL: "https://keycloak.check-consulting.net"
  KEYCLOAK_REALM: "formation"
  KEYCLOAK_CLIENT_ID: "frontend-app"
ingress:
  enabled: false
rollout:
  enabled: true
  strategy:
    blueGreen:
      activeService: front
      previewService: front-preview
      autoPromotionEnabled: false
      scaleDownDelaySeconds: 600
